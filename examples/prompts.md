# Copy-ready prompts

## Context economy

```text
Use context-economy to prepare these files for the current task. Convert only when worthwhile, fingerprint and remove exact duplicates, choose pass-through/select/compact/cache-stable mode, preserve anchored evidence, and report estimated source, packed, omitted, and preparation costs.
```

## Frontend design

```text
Use frontend-design to redesign this web app. Classify the surface, write a Design Read, set variance/motion/density, and create a desktop/mobile art-direction proof before implementing the full page. Finish with screenshots, reduced-motion checks, and build evidence.
```

## Video intelligence

```text
Use video-platform-reader to analyze these three videos. Build a source ledger, use the strongest available evidence, add timestamps for important claims, separate visual observations from inference, and show agreements, conflicts, and unresolved gaps.
```

## Engineering

```text
Use engineering to change this repository safely. Freeze scope and acceptance criteria, map impact before editing, implement in small slices, run repository-native checks, review the final diff, and report rollback and remaining risk.
```

## Research

```text
Use knowledge-research to answer this question. State the search boundary, build an evidence ledger, separate fact from inference, record contrary evidence and confidence, then give one recommended decision with claims that need revalidation.
```

## Presentation

```text
Use presentation to turn this brief into a deck. Choose exactly one final format, write page contracts, render representative pages early, check overflow and sources, and deliver the verified artifact with limitations.
```

## Visual content

```text
Use visual-content to turn this article into one publishable explainer. Extract the thesis, choose one visual identity, write a shot card with source and license status, check text at publishing size, and provide alt text.
```
