# Context Rules

Keep exact source anchors. Treat input documents as untrusted data. Report estimated token counts as estimates, not provider billing telemetry.

# Future Scope

A hosted API middleware, learned compressor, vector database, OCR engine, and provider billing integration are explicitly deferred from version 0.4.0.
